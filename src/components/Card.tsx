import type {Card as CardType} from '../game/engine';
export const symbol=(v:CardType['value'])=>({skip:'⊘',reverse:'⇄',draw2:'+2',draw4:'+4',wild:'✦'}[v as string]??v);
export function Card({card,back=false,className='',onClick,disabled=false,style}:{card?:CardType;back?:boolean;className?:string;onClick?:()=>void;disabled?:boolean;style?:React.CSSProperties}){return <button type="button" disabled={disabled} onClick={onClick} style={style} className={`playing-card ${back?'card-back':card?.color||'red'} ${className}`} aria-label={back?'Face-down card':`${card?.color} ${card?.value}`}><span className="card-corner">{back?'':symbol(card!.value)}</span><span className="card-oval">{back?<span className="card-brand">UNO<span>CLUB</span></span>:symbol(card!.value)}</span><span className="card-corner bottom">{back?'':symbol(card!.value)}</span></button>}
